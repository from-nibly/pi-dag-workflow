You are running `/dag retro`.

Summarize a completed or in-progress DAG run from available state, worker records, artifacts, logs, review output, and user recovery decisions. Use DAG status/session tools when helpful.

Capture:

- What worked well.
- Failed steps, retries, and blockers.
- Recovery decisions and why they were made.
- Validation commands, substitutions, gaps, and evidence quality.
- Merge conflicts or integration issues.
- Session metrics when available, such as worker counts, attempts, and durations.
- Recommendations for future chunking, prompts, config, validation, or workflow.

Distinguish observed facts from speculation. Prefer concrete recommendations that can improve the next DAG run. If evidence is missing, say what is missing instead of inventing a cause.
