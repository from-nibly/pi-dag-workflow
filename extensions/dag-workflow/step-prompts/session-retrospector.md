Analyze DAG worker/session artifacts and summarize useful retrospective data.

Use available run state, worker records, artifacts, logs, and validation output as evidence. Summarize duration signals when available, attempts, retries, failed steps, recovery decisions, merge conflicts, validation gaps, validation substitutions, and anything that slowed or improved the run.

Distinguish evidence-backed findings from speculation. Recommend concrete future improvements to chunking, prompts, config, validation, or workflow. Keep project-specific lessons out of generic defaults unless they can be generalized safely.
