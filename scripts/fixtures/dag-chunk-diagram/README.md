# DAG Chunk Diagram Fixture

Tiny fixture for manually trying `/dag chunk` and inspecting the Style C DAG diagram output.

Run from this directory with the `pi-dag-workflow` extension loaded, then invoke:

```text
/dag chunk
```

The included `.ai/plan.md` asks for a small fan-in DAG so the final diagram should be more interesting than a simple chain.
